# RabbitMQ configuration for EDU Monitor

# Giới hạn memory để tránh OOM trên máy local
vm_memory_high_watermark.relative = 0.4

# Disk free limit
disk_free_limit.absolute = 1GB

# Heartbeat
heartbeat = 60

# Log level
log.console.level = warning
